import React from 'react';
import ReactDOM from 'react-dom';

const element = <h1>Hello World</h1>;
//in reality, we usually render react component 
ReactDOM.render(element, document.getElementById('root'));
